spring:
  application:
    name: auth
  profiles:
    active: dev
  cloud:
    config:
      uri: http://admin:1234@localhost:9000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:1234@localhost:8761/eureka